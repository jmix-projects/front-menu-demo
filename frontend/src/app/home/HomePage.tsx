import React from "react";
import { FormattedMessage } from "react-intl";

const HomePage = () => (
  <div>
    <FormattedMessage id="home.welcome" /> menu-demo!
  </div>
);

export default HomePage;
